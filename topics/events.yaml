topic: events
pipeline: errors
description: Errors data for Snuba
services:
  producers:
    - getsentry/sentry
  consumers:
    - getsentry/snuba
schemas:
  - version: 1
    compatibility_mode: none
    type: json
    resource: events.v1.schema.json
    examples:
      - events/1/
