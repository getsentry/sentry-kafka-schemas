topic: buffered-segment
description: List of buffered spans for a segment
services:
  producers:
    - getsentry/sentry
  consumers:
    - getsentry/sentry
schemas:
  - version: 1
    compatibility_mode: none
    type: json
    resource: buffered-segment.v1.schema.json
    examples:
      - buffered-segment/1/
topic_creation_config:
  compression.type: lz4
