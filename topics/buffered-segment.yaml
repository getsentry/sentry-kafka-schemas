topic: buffered-segment
description: List of buffered spans for a segment
services:
  producers:
    - getsentry/sentry
  consumers:
    - getsentry/sentry
schemas:
  - version: 1
    compatibility_mode: none
    type: json
    resource: snuba-spans.v1.schema.json
    examples:
      - buffered-segment/1/
