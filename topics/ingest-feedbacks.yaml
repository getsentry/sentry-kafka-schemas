topic: ingest-feedbacks
pipeline: replays
description: User feedback events, submitted from user
services:
  producers:
    - getsentry/relay
  consumers:
    - getsentry/sentry
schemas:
  - version: 1
    compatibility_mode: none
    type: json
    resource: events.v1.schema.json
    examples:
      - events/1/
topic_creation_config:
  message.timestamp.type: LogAppendTime
