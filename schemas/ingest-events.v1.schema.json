{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Error message from Relay",
  "type": "object",
  "properties": {
    "type": { "type": "string" },
    "payload": {
      "description": "bytes"
    },
    "event_id": { "type": "string" },
    "project_id": { "type": "integer" }
  },
  "required": ["type", "event_id", "project_id", "payload"]
}
